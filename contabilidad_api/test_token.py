import requests; import json; API_URL = "http://localhost:8000"; resp = requests.post(f"{API_URL}/api/login", data={"username": "admin", "password": "123"}); print("Login status:", resp.status_code); token = resp.json()["access_token"]; print("Token:", token); resp2 = requests.get(f"{API_URL}/api/v1/empresas/todas", headers={"Authorization": f"Bearer {token}"}); print("Empresas status:", resp2.status_code);
